/*global define*/
define([
    './freezeObject'
], function(
    freezeObject) {
    'use strict';

    /**
     * @private
     */
    var RequestScheduler = {
    };

    return RequestScheduler;
});
